// -----------------------------------------------------------------------------
// sisiMove — Registration Schema Barrel
// -----------------------------------------------------------------------------

export { registerUserSchema } from './register-user.schema';

export type {
  RegisterUserFormValues,
  RegisterUserValidatedValues,
} from './register-user.schema';

