// src/domains/assets/domain/value-objects/asset-visibility.enum.ts

export enum AssetVisibility {
  PUBLIC = 'PUBLIC',
  COMMUNITY = 'COMMUNITY',
  CONNECTIONS = 'CONNECTIONS',
  PRIVATE = 'PRIVATE',
}
